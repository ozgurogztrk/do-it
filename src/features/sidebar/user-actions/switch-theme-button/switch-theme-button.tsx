import { IconButton } from "src/components/icon-button";

const SwitchThemeButton = () => {
  // Function to switch between themes
  const switchTheme = () => {};

  return <IconButton icon="lucide:moon-star" onClick={switchTheme} />;
};

export default SwitchThemeButton;
